questions:
- variable: ingress.enabled
  default: true
  description: Use ingress hostname
  type: boolean
  label: Enable Ingress
  show_subquestion_if: true
  group: "Ingress Config"
  subquestions:
  - variable: ingress.hosts[0]
    default: "gitea.local.cloud
    description: "gitea ingress hostname"
    type: string
    label: Ingress URL Hostname
- variable: image.registry
  default: ""
  type: string
  description: "container image registry url"A
  group: "Image Config"

